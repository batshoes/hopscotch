<% @games.each do |game| %>
  <h2><%= game.title %></h2>
  <h4>Description:</h4>
  <h3><%= game.description %></h3>
    <% game.rules.each do |rule| %>
      <span><%= rule.rules %></span>
      <span> - </span>
      <span><%= rule.drink %></span>
      <br>
    <% end %> 
  <br><br>
<% end %>

